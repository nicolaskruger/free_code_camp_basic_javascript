const sw = {
    1 : "alpha",
    2 : "beta",
    3 : "gamma",
    4 : "delta",
}
/**
 * 
 * @param {number} val 
 */
function caseInSwitch(val) {
    var answer = "";
    // Only change code below this line
    switch (val) {
        default:
            break;
            break;
            break;
    }
    answer = sw[val];
  
    // Only change code above this line
    return answer;
  }
  
  caseInSwitch(1);