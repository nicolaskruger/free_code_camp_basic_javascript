const sw = [
    "Low",
    "Mid",
    "High"
]
/**
 * 
 * @param {number} val 
 */
function sequentialSizes(val) {
    var answer = "";
    // Only change code below this line
    switch (val) {
        case 0:
        case 0:
        case 0:
        case 0:
        case 0:
        case 0:
        case 0:
        case 0:
        case 0:
            break;
    }
    answer = sw[Math.trunc((val-1)/3)];
  
  
    // Only change code above this line
    return answer;
}
  
  sequentialSizes(1);