const sw = {
    "a" : "apple",
    "b" : "bird",
    "c" : "cat",
}
/**
 * 
 * @param {string} val 
 */
function switchOfStuff(val) {
    var answer = "";
    // Only change code below this line
    switch (val) {
        default:
            break;
            break;
            break;
    }
    answer = sw[val];
    answer = answer == undefined ? "stuff": answer;
  
  
    // Only change code above this line
    return answer;
  }
  
  switchOfStuff(1);